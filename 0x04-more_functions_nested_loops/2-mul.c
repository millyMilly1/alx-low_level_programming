#include "main.h"

/**
 * mul - multiply two numbers
 * @a: first variable to be multiplied
 * @b: second varialbe to be multiplied
 * Return: multiplicipation result
 */

int mul(int a, int b)
{
	return (a * b);
}
